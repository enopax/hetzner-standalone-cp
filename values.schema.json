{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "A KCM cluster hetzner-standalone-cp template",
  "type": "object",
  "required": [
    "controlPlaneNumber",
    "workersNumber",
    "region",
    "tokenRef"
  ],
  "properties": {
    "clusterAnnotations": {
      "description": "Annotations to apply to the cluster",
      "type": "object",
      "additionalProperties": true
    },
    "clusterLabels": {
      "description": "Labels to apply to the cluster",
      "type": "object",
      "additionalProperties": true
    },
    "clusterNetwork": {
      "description": "The cluster network configuration",
      "type": "object",
      "properties": {
        "pods": {
          "description": "The network ranges from which Pod networks are allocated",
          "type": "object",
          "properties": {
            "cidrBlocks": {
              "description": "A list of CIDR blocks",
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          }
        },
        "services": {
          "description": "The network ranges from which service VIPs are allocated",
          "type": "object",
          "properties": {
            "cidrBlocks": {
              "description": "A list of CIDR blocks",
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          }
        }
      }
    },
    "controlPlane": {
      "description": "The configuration of the control plane machines",
      "type": "object",
      "properties": {
        "additionalSecurityGroups": {
          "description": "Additional security groups to attach",
          "type": "array",
          "items": {
            "type": "object"
          }
        },
        "dataDisks": {
          "description": "Additional data disks to attach",
          "type": "array",
          "items": {
            "type": "object"
          }
        },
        "image": {
          "description": "The name of the Hetzner server image",
          "type": "string"
        },
        "placementGroupName": {
          "description": "The placement group name for control plane nodes",
          "type": "string"
        },
        "type": {
          "description": "The type of Hetzner server to create (e.g., cx21, cpx21, cx31)",
          "type": "string"
        }
      },
      "required": [
        "image",
        "type"
      ]
    },
    "controlPlaneConfig": {
      "description": "The configuration of k0s",
      "type": "object",
      "properties": {
        "args": {
          "description": "Extra arguments passed to the k0s controller",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "downloadURL": {
          "description": "The URL from which to download k0s binary",
          "type": "string"
        },
        "k0s": {
          "description": "k0s configuration",
          "type": "object"
        },
        "postStartCommands": {
          "title": "Post-start commands",
          "description": "Additional commands to run after k0s start",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "preInstallCommands": {
          "title": "Pre-install commands",
          "description": "Additional commands to run before k0s install",
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      }
    },
    "controlPlaneNumber": {
      "description": "The number of the control-plane machines",
      "minimum": 1,
      "type": "integer"
    },
    "k0s": {
      "description": "k0s specific settings",
      "type": "object",
      "properties": {
        "version": {
          "description": "The k0s version to use",
          "type": "string"
        }
      },
      "required": [
        "version"
      ]
    },
    "labels": {
      "description": "Labels to apply to Hetzner resources",
      "type": "object",
      "additionalProperties": true
    },
    "nodeClasses": {
      "description": "Additional Machine Classes for worker nodes",
      "type": "array",
      "items": {
        "type": "object"
      }
    },
    "placementGroupName": {
      "description": "Name of the placement group to use for nodes",
      "type": "string"
    },
    "region": {
      "description": "The Hetzner region name (e.g., fsn1, nbg1, hel1)",
      "type": "string"
    },
    "sshKeyNames": {
      "description": "SSH key names to use for cluster nodes",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "tokenRef": {
      "description": "Reference to the secret containing Hetzner API token",
      "type": "object",
      "required": [
        "name",
        "key"
      ],
      "properties": {
        "key": {
          "description": "Key in the secret containing the token",
          "type": "string"
        },
        "name": {
          "description": "Name of the secret",
          "type": "string"
        }
      }
    },
    "worker": {
      "description": "The configuration of the worker machines",
      "type": "object",
      "properties": {
        "additionalSecurityGroups": {
          "description": "Additional security groups to attach",
          "type": "array",
          "items": {
            "type": "object"
          }
        },
        "dataDisks": {
          "description": "Additional data disks to attach",
          "type": "array",
          "items": {
            "type": "object"
          }
        },
        "image": {
          "description": "The name of the Hetzner server image",
          "type": "string"
        },
        "placementGroupName": {
          "description": "The placement group name for worker nodes",
          "type": "string"
        },
        "type": {
          "description": "The type of Hetzner server to create (e.g., cx21, cpx21, cx31)",
          "type": "string"
        }
      },
      "required": [
        "image",
        "type"
      ]
    },
    "workerConfig": {
      "description": "The configuration of the k0s worker",
      "type": "object",
      "properties": {
        "postStartCommands": {
          "title": "Post-start commands",
          "description": "Additional commands to run after k0s start",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "preInstallCommands": {
          "title": "Pre-install commands",
          "description": "Additional commands to run before k0s install",
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      }
    },
    "workersNumber": {
      "description": "The number of the worker machines",
      "minimum": 0,
      "type": "integer"
    }
  }
}
